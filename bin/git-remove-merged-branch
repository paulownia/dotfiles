#!/bin/bash -e

BRANCH=${1}

if [[ -z $BRANCH ]]; then
	BRANCH=$(git symbolic-ref --short HEAD)
fi

git fetch origin
git remote prune origin
git branch --merged "${BRANCH}" | grep -v '\*' | grep "/" | xargs git branch -d
