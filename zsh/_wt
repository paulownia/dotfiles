#compdef wt

if ! git rev-parse --is-inside-work-tree >/dev/null 2>&1; then
	_message "not inside a git repository"
	return
fi

local -a WORKTREES
WORKTREES=( $(git worktree list --porcelain | awk '/^branch refs\/heads\// { print substr($0, 19) }') )

_describe "Worktrees" WORKTREES
